groups:
  - name: ai-medical-api
    rules:
      - alert: ApiHighErrorRate
        expr: sum(rate(http_requests_total{status=~"5.."}[5m])) / clamp_min(sum(rate(http_requests_total[5m])), 0.001) > 0.05
        for: 10m
        labels:
          severity: critical
        annotations:
          summary: "AI medical API 5xx rate exceeds 5%"
      - alert: ApiHighLatency
        expr: histogram_quantile(0.95, sum by (le) (rate(http_request_duration_seconds_bucket[5m]))) > 2
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "AI medical API p95 latency exceeds 2 seconds"
      - alert: ApiTargetDown
        expr: up{job="ai-medical-api"} == 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "AI medical API is unavailable"


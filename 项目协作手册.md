# AI 智能医疗问诊平台 — 项目协作手册

> **写给谁看：** 产品、开发、测试、运维、业务用户（患者 / 医生 / 管理员）  
> **怎么读：** 先看「一分钟读懂」，再按你的角色跳到对应章节；技术细节集中在后半部分。  
> **相关文档：** 部署细节见 [DEPLOY.md](./DEPLOY.md)；域名上线见 [域名部署文档.md](./域名部署文档.md)；小程序上架见 [微信小程序上架指南.md](./微信小程序上架指南.md)；更细的架构笔记见 [note.md](./note.md)。

---

## 0. 一分钟读懂

这是一套 **面向患者、医生、管理员的在线医疗辅助系统**，核心卖点是：

1. **AI 问诊**：患者用自然语言提问，系统结合「知识库检索 + 大模型」流式回答  
2. **症状推理**：患者勾选症状，系统用 **知识图谱** 给出可能疾病与建议科室  
3. **人工链路**：在线咨询医生、预约挂号、医生维护健康档案  
4. **运营后台**：账号、科室、知识库、图谱、文章公告、数据看板

**重要声明（产品与法务共识）：**  
AI 回答与图谱推理 **仅供健康参考，不能替代执业医师诊断**。产品文案、测试用例、用户告知均需体现这一点。

```
患者 ──► 门户（问诊 / 图谱 / 咨询 / 预约）
医生 ──► 工作台（回咨询 / 改预约 / 写档案）
管理 ──► 后台（人、知识、运营数据）
         │
         ▼
   FastAPI + MySQL + Neo4j + 向量库 + LLM
```

---

## 1. 读者导航（按角色）

| 你的角色 | 建议优先阅读 | 你要交付 / 关注的事 |
|----------|--------------|---------------------|
| **产品** | §2 业务目标、§3 角色与场景、§4 功能地图、§10 边界与风险 | 需求优先级、状态机、验收标准 |
| **开发** | §5 架构、§6 权限、§7 核心链路、§8 数据与 API | 改代码时找对模块、不破坏鉴权与级联 |
| **测试** | §3～4、§9 状态约定、§11 测试清单 | 三角色冒烟、AI/知识库依赖环境 |
| **运维** | §5.3 部署形态、§12 运维手册、DEPLOY.md | 一键 Compose、Ollama/域名、备份与排障 |
| **用户** | §3 + §13 使用指南 | 怎么注册登录、各端能做什么 |

---

## 2. 业务目标与价值主张

### 2.1 要解决什么问题

| 痛点 | 平台怎么解决 |
|------|----------------|
| 轻症咨询门槛高、等待久 | AI 24 小时初步答疑 + 知识库引用 |
| 「有哪些可能疾病」不直观 | 症状 → 图谱推理 → 建议科室 |
| 医患沟通难留痕 | 在线咨询工单 + 预约状态流转 |
| 运营缺统一后台 | 账号 / 知识 / 内容 / 数据一屏管理 |

### 2.2 不做的事（边界）

- 不做医保结算、处方开具合规流转、远程处方配送  
- 不做实时音视频问诊  
- AI **不保证**医学正确性；严重症状文案引导线下就医  
- 知识图谱症状抽取当前是 **关键词匹配**，不是医学 NER

### 2.3 成功指标（产品可量化）

| 指标 | 含义 |
|------|------|
| AI 会话数 / 日 | 智能问诊使用量 |
| 知识库向量化成功率 | 运营知识是否真正进检索 |
| 咨询待回复积压 | 医生侧响应能力 |
| 预约完成率 | 线下就诊转化 |
| 系统可用性 | 前端可访问 + 后端 healthy + LLM 可达 |

---

## 3. 角色与使用场景

系统采用 **分表角色**（患者 / 医生 / 管理员各一张账号表），登录时必须选择对应身份。另有 **超级管理员 root**（管理员表中的特殊标记），拥有管理后台全部权限，并可在「用户管理」中统一管理用户、医生、普通管理员。

### 3.1 角色对照表

| 角色 | 登录身份 | 首页 | 谁可以创建 | 典型用户 |
|------|----------|------|----------|----------|
| 患者 `user` | 用户 | `/portal/home` | 自助注册 / 后台创建 | 求医者、健康咨询者 |
| 医生 `doctor` | 医生 | `/doctor/dashboard` | 注册（选科室）/ 后台创建 | 科室医生 |
| 管理员 `admin` | 管理员 | `/admin/dashboard` | 注册 / 超级管理员创建 | 运营、内容、知识库维护 |
| 超级管理员 `root` | 管理员 | `/admin/dashboard` | 启动脚本自动保证 | 系统最高权限 |

**演示账号（空库首次初始化后）：**

| 用户名 | 密码 | 登录身份 | 说明 |
|--------|------|----------|------|
| `admin` | `feng1010` | 管理员 | root 超级管理员（每次后端启动会确保存在） |
| `doctor` / `doctor2` | `123456` | 医生 | 演示医生 |
| `user` | `123456` | 用户 | 演示患者 |

> 登录身份必须与账号类型一致，否则会提示「登录身份不正确」。

### 3.2 场景故事（给产品 / 用户）

**场景 A：患者半夜不适**  
注册登录 → AI 问诊描述症状 → 查看引用来源 → 可选「症状推理」加症状标签 → 仍不放心则「在线咨询」选医生 → 有需要再「预约挂号」。

**场景 B：医生上班**  
登录医生台 → 处理待回复咨询 → 确认今日预约 → 为接诊患者写健康档案。

**场景 C：运营更新知识**  
管理员上传 PDF/Word/TXT → 等待向量化成功 → AI 问诊即可检索到新内容；同时可维护文章、公告、科室与医生。

---

## 4. 功能地图（产品视角）

### 4.1 患者门户 `/portal`

| 功能 | 用户感知 | 依赖 |
|------|----------|------|
| 首页 | 个人统计、快捷入口、公告 | 统计 API、公告 |
| AI 问诊 | 多轮对话、流式打字、引用与图谱提示 | LLM + 向量库 +（可选）图谱 |
| 症状推理 | 选症状 → 可能疾病 / 科室 | Neo4j |
| 在线咨询 | 提交主诉、查看医生回复 | MySQL 工单 |
| 预约挂号 | 科室→医生→日期时段 | MySQL |
| 健康档案 | 只读查看医生写入的档案 | MySQL |
| 健康资讯 / 公告 | 阅读科普与通知 | MySQL |
| 个人中心 | 资料、密码、头像 | 上传目录 |

### 4.2 医生工作台 `/doctor`

| 功能 | 说明 |
|------|------|
| 工作台 | 待回复、今日预约、患者数等概览 |
| 待回复咨询 | 回复并认领未分配工单 |
| 我的预约 | 改预约状态（确认 / 完成 / 取消） |
| 患者档案 | 为自己的患者建档 / 编辑 |
| 个人中心 | 职称、专长、简介等 |

### 4.3 管理后台 `/admin`

| 功能 | 说明 |
|------|------|
| 数据概览 | 总量与趋势图表（ECharts） |
| 用户管理 | 聚合展示用户/医生/管理员；**root 可统一增删改启停**；普通管理员主要管患者 |
| 医生管理 | 医生账号与科室绑定 |
| 科室管理 | CRUD；有医生挂靠时不可删 |
| 知识库 | 上传、向量化状态、重试、删除 |
| 知识图谱 | 可视化、搜索、子图 |
| 咨询 / 预约管理 | 全平台工单与预约运维 |
| 文章 / 公告 | 发布与下架 |
| 个人中心 | 管理员资料 |

---

## 5. 技术架构（开发 / 运维）

### 5.1 分层一览

```
浏览器（Vue 3 + Element Plus + Pinia）
        │  HTTP / SSE    /api/v1/*   /uploads33/avatar/*
        ▼
Nginx 前端容器（静态资源 + 反代）
        ▼
FastAPI 后端（鉴权、业务、RAG、图谱）
   ├── MySQL          业务数据 db_ai_medical
   ├── Chroma         知识库向量 medical_knowledge
   ├── Neo4j          疾病-症状等知识图谱
   └── LLM / Embedding
         ├─ 本地：宿主机 Ollama（Docker 经 host.docker.internal）
         └─ 云端：阿里云 MaaS OpenAI 兼容接口
```

### 5.2 技术栈速查

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | Vue 3、Vite、Element Plus、Pinia、Vue Router、ECharts、markdown-it | SPA；主题为浅紫红 |
| 后端 | FastAPI、SQLAlchemy、JWT、LangChain、Chroma、Neo4j Driver | Python 3.11 |
| 存储 | MySQL 8、Neo4j 5、Chroma 持久化卷、上传卷 | Compose 编排 |
| 构建 | Docker Compose 统一 `docker-compose.yml` | 用不同 `.env` 切环境 |

### 5.3 三种部署形态（运维必读）

| 场景 | 命令 | 配置 |
|------|------|------|
| 本地开发 | `docker compose up -d --build` | `.env`（默认 `with-mysql,with-graph`） |
| 阿里云单机 | `docker compose --env-file .env.prod up -d --build` | 复制 `.env.prod.example` |
| 阿里云共享 MySQL | `docker compose --env-file .env.shared up -d --build` | 复制 `.env.shared.example`，需 `shared-infra` 网络 |

中间件用 **Compose Profiles** 控制：`with-mysql`、`with-graph`。

### 5.4 仓库目录（开发找文件）

```
AI_Consultation/
├── client/                 # 前端
│   ├── src/views/auth|portal|doctor|admin
│   ├── src/layouts/        # PortalLayout / AdminLayout
│   ├── src/styles/global.css
│   └── nginx.conf
├── server/
│   ├── api/v1/             # 接口路由
│   ├── services/           # 认证、RAG、图谱、统计
│   ├── models/ schemas/ rag/
│   └── scripts/            # 启动入口、seed、图谱/知识初始化
├── docker-compose.yml
├── .env.example / .env.prod.example / .env.shared.example
├── DEPLOY.md
└── 域名部署文档.md
```

---

## 6. 权限与安全（开发 / 测试）

### 6.1 登录与 Token

1. 前端提交 `username + password + role`  
2. 后端按角色查对应表，校验密码哈希与 `status`  
3. 签发 JWT（约 24 小时），载荷含 `user_id`、`username`、`role`  
4. 后续请求：`Authorization: Bearer <token>`  
5. `root` 在 JWT 中为 `role=root`，可访问所有 `require_roles("admin")` 接口  

密码使用哈希存储（非明文）。账号禁用后无法登录；公开医生列表只展示启用医生。

### 6.2 前端路由守卫

- 未登录 → `/login`  
- 已登录访问登录/注册 → 角色首页  
- 访问非本角色路由 → 踢回本角色首页  
- `root` 可进入 `/admin/*`  

### 6.3 超级管理员能力边界

| 能力 | root | 普通 admin |
|------|------|------------|
| 管理后台全部菜单 | ✅ | ✅ |
| 用户管理中编辑/禁用/删除 **患者** | ✅ | ✅ |
| 用户管理中管理 **医生 / 普通管理员** | ✅ | ❌（医生请去医生管理） |
| 操作超级管理员自身账号 | ❌ 禁止 | ❌ |
| `/admins/*` 管理员 CRUD | ✅ | ❌ |

### 6.4 安全注意点

- 生产务必修改：`JWT_SECRET_KEY`、数据库与 Neo4j 密码、root 密码策略  
- 公网只暴露前端端口；MySQL / Neo4j / 后端建议绑 `127.0.0.1`  
- 知识库文件 **不** 作为公开静态目录暴露；头像走 `/uploads33/avatar`  
- AI 回复前端做 Markdown 消毒，降低 XSS 风险  

---

## 7. 核心智能链路（产品 + 开发）

### 7.1 AI 问诊（RAG + LLM + 可选图谱）

```
用户提问
  → 创建/绑定会话，写入用户消息
  → 向量检索 Top-K 知识片段
  → 从问题中匹配常见症状词 → Neo4j 辅助疾病/科室
  → 拼装系统提示（含「仅供参考」）
  → LLM 流式输出（SSE：session / content / done / error）
  → 写入助手消息（引用、图谱结果、耗时）
```

**产品含义：** 知识库越全、向量化越成功，回答越「有据」；LLM 挂了则整条链路不可用。

### 7.2 知识库入库

```
上传文件 → 落盘 → 记录 vector_status
  0 待处理 → 1 处理中 → 2 成功 / 3 失败
解析 → 分块 → Embedding → 写入 Chroma
```

**运维常见误区：** 列表里「失败」多半是 **嵌入服务连不上**（Ollama/MaaS），不是上传接口失败。

### 7.3 症状推理（图谱）

用户选症状 → Cypher 按命中症状数排序疾病 → 展示匹配度与建议科室；可再查疾病详情（药物、检查、宜忌等）。

图谱启动时可 `INIT_GRAPH=true` 导入约数十种常见病种关系。

---

## 8. 数据与 API（开发速查）

### 8.1 主要业务表

| 表 | 用途 |
|----|------|
| `t_user` / `t_doctor` / `t_admin` | 三角色账号（admin 含 `admin_role=root/admin`） |
| `t_department` | 科室 |
| `t_consult_session` / `t_consult_message` | AI 会话与消息 |
| `t_doctor_consult` / `t_doctor_reply` | 人工咨询 |
| `t_appointment` | 预约 |
| `t_health_record` | 健康档案 |
| `t_knowledge_file` / `t_knowledge_chunk` | 知识库元数据与分块 |
| `t_article` / `t_notice` | 资讯与公告 |

关联多由业务代码维护（删用户/医生时级联清理会话、咨询、预约、档案等）。

### 8.2 API 前缀 `/api/v1`

统一响应：`{ code, message, data }`。

| 前缀 | 模块 |
|------|------|
| `/auth` | 登录、注册 |
| `/users` `/admins` `/doctors` `/departments` | 账号与组织 |
| `/knowledge` `/chat` `/graph` | 智能能力 |
| `/consult` `/appointments` `/records` | 医患业务 |
| `/articles` `/notices` `/stat` `/profile` | 内容、统计、个人 |

本地开发：Vite 将 `/api`、`/uploads33` 代理到 `127.0.0.1:8000`。

---

## 9. 状态机约定（产品 / 测试必记）

| 对象 | 取值 | 含义 |
|------|------|------|
| 账号 `status` | 1 / 0 | 正常 / 禁用 |
| 知识库 `vector_status` | 0→1→2/3 | 待处理→处理中→成功/失败 |
| 人工咨询 `status` | 0 / 1 | 待回复 / 已回复 |
| 预约 `status` | 0 / 1 / 2 / 3 | 待确认 / 已确认 / 已完成 / 已取消 |

预约推荐流转：`待确认 → 已确认 → 已完成`，随时可 `已取消`。

---

## 10. 边界、风险与合规（全员）

| 风险 | 说明 | 缓解 |
|------|------|------|
| 医疗误导 | AI 幻觉 | 文案免责；引导就医；知识库质量 |
| 密钥泄露 | `.env`、演示密码 | 生产改密；勿提交真实 Key |
| LLM 不可用 | Ollama 未启 / Key 失效 | 监控 `/health` + 对话冒烟 |
| 向量化失败 | 嵌入不可达 | 查 backend 日志 `[RAG]`；重试向量化 |
| 注册页白屏 | 前端 chunk 缓存 | 重建 frontend；强刷；index.html 不缓存 |
| 低配 ECS OOM | Neo4j + MySQL + 后端 | 用 `.env.prod` 内存限制；或共享 MySQL |

---

## 11. 测试清单（测试）

### 11.1 冒烟（每次发版至少跑）

1. 三角色登录成功；错身份登录失败  
2. 患者：AI 问诊能流式出字；症状推理有结果（图谱 healthy 时）  
3. 患者：发起咨询、创建预约  
4. 医生：回复咨询、改预约、写档案  
5. 管理员：上传知识库 → 最终 `成功`（依赖 Embedding）  
6. root：用户管理可编辑医生/管理员；不可删 root  
7. 未登录访问 `/portal` 跳转登录  

### 11.2 重点回归

| 模块 | 用例要点 |
|------|----------|
| 注册 | 用户/医生/管理员注册后，用对应身份登录 |
| 删除用户 | 关联会话/咨询/预约/档案被清理 |
| 科室删除 | 有医生时禁止删除 |
| 知识库 | 失败可重试；删除后 AI 不再引用该文件 |
| 权限 | 医生进不了 `/admin`；患者进不了 `/doctor` |
| 分页 | 各列表分页在右下角；用户管理序号连续 |

### 11.3 环境依赖说明（测 AI 前先确认）

```bash
# 容器健康
docker compose ps

# 容器能否访问 Ollama（本地方案）
docker compose exec backend curl -s http://host.docker.internal:11434/api/tags
```

---

## 12. 运维手册摘要

### 12.1 本地快速启动

```bash
# 1. 宿主机先起 Ollama，并拉取对话模型 + 嵌入模型
export OLLAMA_HOST=0.0.0.0:11434
ollama serve
ollama pull qwen3:latest && ollama cp qwen3:latest qwen3.6-plus
ollama pull nomic-embed-text

# 2. 配置并启动
cp .env.example .env
docker compose up -d --build

# 3. 访问
# 前端 http://localhost
# API 文档 http://localhost:8000/docs
```

### 12.2 常用命令

```bash
docker compose ps
docker compose logs -f backend
docker compose up -d --build frontend   # 只更前端
docker compose up -d --build backend    # 只更后端
docker compose exec backend python scripts/ensure_root.py
docker compose down                     # 停服务保留数据
docker compose down -v                  # 清空卷（慎用）
```

### 12.3 端口默认值

| 端口 | 用途 | 变量 |
|------|------|------|
| 80 | 前端 | `WEB_PORT`（生产常 9000） |
| 8000 | 后端 | `BACKEND_PORT` / `BACKEND_PUBLISH` |
| 3308 | MySQL 宿主机映射 | `MYSQL_PORT` |
| 7474 / 7687 | Neo4j | `NEO4J_*_PORT` |
| 11434 | Ollama（宿主机） | — |

### 12.4 排障速查

| 现象 | 先查什么 |
|------|----------|
| 页面打不开 | `docker compose ps`、WEB_PORT、安全组 |
| 登录失败 | 角色是否选对、账号是否禁用、backend 日志 |
| AI 无回复 | Ollama/MaaS、`DOCKER_OPENAI_BASE_URL`、backend 日志 |
| 知识库一直失败 | Embedding 连通性、模型名、磁盘权限 |
| 图谱空 / 推理无结果 | Neo4j 是否 healthy、`INIT_GRAPH`、内存是否被杀 |
| 免费注册跳转失败 | 重建前端、强刷缓存 |

域名与 HTTPS 步骤见 `域名部署文档.md`。

---

## 13. 用户使用指南（短版）

### 13.1 患者

1. 打开网站 → **免费注册**（选「用户」）→ 登录时身份选「用户」  
2. **AI 问诊**：直接提问；回答仅供参考  
3. **症状推理**：添加症状标签查看可能疾病  
4. **在线咨询 / 预约挂号**：需要真人医生时使用  
5. **健康档案 / 资讯**：查看医生记录与科普  

### 13.2 医生

1. 登录身份选「医生」  
2. 优先处理「待回复咨询」  
3. 在「我的预约」中确认或完成预约  
4. 在「患者档案」中维护诊断与处方记录  

### 13.3 管理员 / 超级管理员

1. 登录身份选「管理员」  
2. 维护科室与医生，保证预约链路可用  
3. 上传知识库并确认状态为「成功」  
4. 发布文章与公告；查看数据概览  
5. 超级管理员可在「用户管理」统一管理三类账号  

---

## 14. 协作建议（怎么一起干活）

| 环节 | 建议 |
|------|------|
| 需求 | 产品按「场景 + 状态机 + 角色」出 PRD；标清是否依赖 LLM/图谱 |
| 开发 | 改权限先改 `roles` + 前后端守卫；删账号注意级联 |
| 联调 | 先保证 MySQL healthy，再图谱，再 Ollama；不要三者同时排 |
| 测试 | 环境 checklist 进用例；AI 相关用例标注「依赖外部模型」 |
| 发版 | 改前端必 `--build frontend`；改 Nginx/域名配置后重建镜像 |
| 运维 | 生产用 `.env.prod` / `.env.shared`；备份 mysql/neo4j/chroma/uploads 四卷 |

---

## 15. 文档索引

| 文档 | 适合谁 | 内容 |
|------|--------|------|
| **本文** | 全员 | 业务 + 技术总览与协作约定 |
| [DEPLOY.md](./DEPLOY.md) | 运维 / 开发 | Docker、Ollama、环境变量、排障 |
| [域名部署文档.md](./域名部署文档.md) | 运维 | 备案域名、宿主机 Nginx、HTTPS |
| [微信小程序上架指南.md](./微信小程序上架指南.md) | 运维 / 产品 | AppID、合法域名、医疗类目、提审 |
| [note.md](./note.md) | 开发 | 更细的业务逻辑与架构笔记 |
| `.env.*.example` | 运维 | 各环境变量模板 |
| `http://<host>:8000/docs` | 开发 / 测试 | OpenAPI 在线文档 |

---

## 16. 版本与维护说明

- 前端主题：浅紫红（品牌色约 `#cc5084`）  
- Compose：单一 `docker-compose.yml` + 多套 env，不再维护多份 compose 叠加文件  
- Root 账号：由 `server/scripts/ensure_root.py` 在后端启动时保证  

若本文与代码行为冲突，**以当前仓库代码与 OpenAPI（`/docs`）为准**，并请同步回改本文对应章节。

---

*文档目的：让产品说清「做什么」、开发找对「改哪里」、测试知道「测什么」、运维清楚「怎么挂」、用户明白「怎么用」。*

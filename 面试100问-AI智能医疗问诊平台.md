# AI 智能医疗问诊平台 — 业务技术解析 & 面试 100 问

> 用途：项目复盘 + 面试准备。答案均按**本仓库真实实现**撰写；不确定处已标明「可改进点」，面试时主动说比硬编更加分。  
> 建议用法：先读「一、业务」「二、技术栈」，再按岗位重点刷对应题号（后端偏 26–40、81–96；AI 偏 51–80；全栈全刷）。

---

## 一、业务功能逻辑（面试开场 2 分钟版）

### 1.1 项目定位

面向 **患者、医生、管理员** 的 B/S 医疗辅助系统。核心价值：

| 能力 | 业务含义 |
|------|----------|
| AI 问诊 | 患者自然语言提问 → 知识库检索增强 + 大模型流式回答 |
| 症状推理 | 勾选/输入症状 → Neo4j 图谱给出可能疾病与建议科室 |
| 人工闭环 | 在线咨询工单、预约挂号、医生写健康档案 |
| 运营后台 | 账号/科室/知识库/图谱/文章公告/数据看板 |

**免责声明（必说）：** AI 与图谱结果仅供健康参考，不替代执业医师诊断。

### 1.2 角色与入口

| 角色 | 表 | 登录身份 | 首页 | 主要能力 |
|------|-----|----------|------|----------|
| 患者 user | `t_user` | 用户 | `/portal/*` | AI 问诊、症状推理、咨询、预约、档案只读、资讯 |
| 医生 doctor | `t_doctor` | 医生 | `/doctor/*` | 回咨询、改预约、写档案 |
| 管理员 admin | `t_admin` | 管理员 | `/admin/*` | 运营与知识工程 |
| 超管 root | `t_admin.admin_role=root` | 管理员 | `/admin/*` | 继承 admin，并可统一管用户/医生/普通管理员 |

登录必须选对身份；选错提示「登录身份不正确」。密码为 PBKDF2 哈希（兼容历史明文校验）。

### 1.3 患者端主流程

```
注册/登录(user)
  ├─ AI问诊：建会话 → 写用户消息 → RAG+图谱上下文 → SSE流式回复 → 落库助手消息
  ├─ 症状推理：选症状 → /graph/infer → 疾病排序+科室
  ├─ 在线咨询：选医生+主诉 → 工单 status=0 → 医生回复 status=1
  ├─ 预约挂号：科室→医生→日期时段 → status 0待确认→1已确认→2已完成 / 3取消
  └─ 健康档案：只读医生写入的记录
```

### 1.4 医生端主流程

```
登录(doctor)
  ├─ 待回复：本人或未分配且 status=0 的工单 → 回复时认领
  ├─ 预约：更新状态
  └─ 患者档案：仅操作 doctor_id=自己 的档案
```

### 1.5 管理端主流程

```
登录(admin/root)
  ├─ 用户管理：聚合三表账号；root 可管医生/管理员；列表用连续序号
  ├─ 医生/科室/文章/公告 CRUD
  ├─ 知识库：上传 → 异步向量化(0→1→2/3) → 可重试/删除
  ├─ 知识图谱可视化
  └─ 咨询/预约运维 + ECharts 看板
```

### 1.6 关键状态机（高频考点）

| 对象 | 状态 |
|------|------|
| 账号 status | 1 正常 / 0 禁用 |
| 知识库 vector_status | 0 待处理 → 1 处理中 → 2 成功 / 3 失败 |
| 人工咨询 | 0 待回复 / 1 已回复 |
| 预约 | 0 待确认 / 1 已确认 / 2 已完成 / 3 已取消 |

---

## 二、技术栈与架构

### 2.1 总体架构

```
浏览器 Vue3 + Element Plus + Pinia
        │ HTTP / SSE  /api/v1  /uploads33/avatar
        ▼
Nginx(前端容器) 反代 → FastAPI(backend)
        ├─ MySQL：业务数据
        ├─ Chroma：知识向量
        ├─ Neo4j：知识图谱
        └─ LLM/Embedding：Ollama 或阿里云 MaaS
```

### 2.2 技术选型一览

| 层 | 技术 | 用途 |
|----|------|------|
| 前端 | Vue3、Vite、Router、Pinia、Element Plus、ECharts、markdown-it | SPA、看板、AI Markdown |
| 后端 | FastAPI、SQLAlchemy2、Pydantic、python-jose | REST + SSE |
| AI | LangChain ChatOpenAI、Chroma、文档解析 pypdf/docx | RAG |
| 图谱 | Neo4j Driver + Cypher | 症状推理 |
| 部署 | Docker Compose、Nginx | 多环境 Profiles |

### 2.3 后端分层

`api/v1` 路由 → `services` 业务 → `models` ORM → `schemas` 入参出参；`rag/` 负责加载、分块、向量库；`core/` 配置、JWT、依赖注入、角色扩展。

### 2.4 前端关键设计

- `baseURL: /api/v1`，Axios 自动带 Bearer Token；401 清登录态  
- AI 问诊用 `fetch` + ReadableStream（SSE），不走 Axios  
- 路由守卫：未登录跳登录；角色隔离；root 可进 admin  
- 登录/注册静态导入，避免懒加载 chunk 缓存白屏  

---

## 三、面试 100 问 & 详细答案

### A. 项目与业务（1–15）

**1. 用 1 分钟介绍这个项目。**  
答：这是面向患者、医生、管理员的 AI 医疗问诊平台。患者可 AI 问诊、症状图谱推理、在线咨询和预约；医生处理咨询与档案；管理员维护知识库与运营数据。技术上是 Vue3 + FastAPI，智能能力是 RAG + 大模型 SSE + Neo4j。AI 仅供参考，不替代诊疗。

**2. 为什么要做三角色而不是单用户表加 role 字段？**  
答：三类账号字段差异大（患者有过敏史年龄、医生有科室职称、管理员有 admin_role），分表更清晰；登录时强制选身份，避免同一用户名跨角色混淆。代价是列表要聚合三表，用户管理用 `seq` 序号展示，操作仍用真实 id+role。

**3. 患者从进站到看完病，完整路径是什么？**  
答：注册登录 → AI 问诊初步了解 → 症状推理看可能疾病/科室 → 在线咨询真人医生 → 预约挂号 → 医生写健康档案，患者只读查看。同时可看资讯公告。

**4. AI 问诊和症状推理有什么区别？**  
答：AI 问诊是自然语言 + RAG + LLM 生成解释性回答；症状推理是结构化选症状，走图谱共现统计，结果可解释、偏规则。问诊里也会抽症状词注入图谱上下文，两者互补。

**5. 「在线咨询」状态怎么流转？**  
答：患者创建工单 `status=0`；医生回复后 `status=1`；未指定医生的工单可被医生认领（首次回复写 doctor_id）。

**6. 预约状态机如何设计？为什么？**  
答：0 待确认 → 1 已确认 → 2 已完成，另可 3 已取消。对齐线下挂号：先确认、再就诊完成；取消独立出口避免污染完成率。

**7. 超级管理员和普通管理员差在哪？**  
答：JWT 中 root vs admin；root 继承所有 admin 接口权限；仅 root 可调 `/admins` CRUD；用户管理页 root 可直接管医生/管理员；不能操作 root 自身账号。

**8. 知识库上传成功但显示失败，业务上怎么理解？**  
答：文件已落盘、元数据已有，但向量化失败（`vector_status=3`），常见原因是 Embedding 服务（Ollama）不可达。可重试向量化，不是「上传接口挂了」。

**9. 删除患者会清哪些数据？**  
答：AI 会话与消息、人工咨询与回复、预约、健康档案。由业务代码级联删除（ORM 未强制 FK）。

**10. 科室为什么不能随意删？**  
答：有医生挂靠时禁止删除，避免预约/医生数据悬空，保证组织完整性。

**11. 项目明确不做哪些事？**  
答：不做医保结算、合规电子处方流转、音视频问诊；不做医学 NER（症状抽取是关键词匹配）。面试主动说边界显得务实。

**12. 如何体现「医疗安全」？**  
答：Prompt 含仅供参考与就医引导；前端 Markdown 消毒防 XSS；权限隔离会话仅本人可见；禁用账号无法登录。

**13. 运营数据看板看什么？**  
答：用户/医生/咨询/预约等总量，近 7 日趋势，科室预约分布，知识库文件类型分布（ECharts）。

**14. 演示账号怎么记？**  
答：`admin/feng1010` 超管（启动 ensure_root）；`doctor`/`user` 等演示账号密码多为 `123456`（以环境为准）。

**15. 若产品要加「收藏问诊结果」，你会怎么设计？**  
答：新表 `t_favorite`（user_id, message_id/session_id）；接口鉴权仅本人；列表分页；不改动 SSE 主链路。体现扩展性思维。

---

### B. 架构与工程（16–25）

**16. 为什么选 FastAPI 而不是 Django？**  
答：原生 async、StreamingResponse 做 SSE 自然；OpenAPI 自动文档；Pydantic 校验适合 API 项目。本项目偏接口与流式，而非重模板 CMS。

**17. 为什么选 Vue3 + Vite？**  
答：Composition API 适合中大型 SPA；Vite 启动快；Element Plus 管理端表格表单成熟；Pinia 管理登录态清晰。

**18. 前后端如何通信？**  
答：REST JSON，统一 `{code,message,data}`；鉴权 Bearer JWT；AI 问诊 SSE；头像 `/uploads33/avatar`。开发时 Vite 代理 `/api` 到 8000。

**19. 后端分层怎么划分？**  
答：Router 只做参数与依赖注入；Service 承载业务；Model 映射表；Schema 校验；RAG/Graph 独立服务，避免 Router 臃肿。

**20. 为什么 MySQL + Neo4j + Chroma 三套存储？**  
答：MySQL 事务型业务；Neo4j 多跳关系推理；Chroma 向量相似度检索。各司其职，避免硬塞一种库。

**21. 单进程 uvicorn 的原因？**  
答：SSE 长连接 + 本地 Chroma 文件库，多 worker 易冲突或重复加载；生产保持单 worker，水平扩展需换共享向量方案。

**22. 统一异常怎么做？**  
答：`HTTPException` 与 `RequestValidationError` 转中文 `message`，前端 Axios 拦截弹窗，降低联调成本。

**23. 配置如何管理？**  
答：`core/config.py` + `.env`；Compose 注入 `DOCKER_OPENAI_BASE_URL` 等；本地/生产/共享 MySQL 用不同 env-file。

**24. 登录与注册为何静态 import？**  
答：曾出现懒加载 chunk 哈希变更导致「免费注册」白屏；打入主包 + Nginx 对 index.html 禁长缓存。

**25. 你如何保证可维护性？**  
答：分层、角色常量集中、状态码约定、DEPLOY/协作手册、健康检查与 RAG 日志标签。

---

### C. 认证安全权限（26–40）

**26. JWT 载荷有什么？**  
答：`sub`(用户名)、`user_id`、`role`、`exp`。前端以 JWT 角色为准，不只信 localStorage。

**27. Token 过期怎么办？**  
答：后端 401；前端拦截清 token 跳登录。默认约 24 小时（看 `jwt_expire_minutes`）。

**28. 密码如何存储？**  
答：PBKDF2-SHA256，格式 `pbkdf2_sha256$salt$digest`，约 12 万轮；`hmac.compare_digest` 防时序攻击；兼容旧明文并在登录成功后可升级哈希。

**29. 为什么登录要传 role？**  
答：同名可能存在不同表；按角色查对应表。若密码对但 role 不对，提示身份不正确（并可能提示真实角色）。

**30. `require_roles` 如何工作？**  
答：依赖注入取当前用户；`expand_required_roles` 在需要 admin 时自动加入 root，实现超管继承。

**31. 禁用账号如何立即失效？**  
答：`get_current_user` 查库校验 `status==0` 则 403，不只信 JWT。

**32. 会话越权如何防？**  
答：拉消息、续聊均过滤 `user_id == current.user_id`。

**33. 前端路由守卫做了什么？**  
答：公开页放行；已登录访问登录/注册回首页；未登录跳登录；role 不匹配踢回角色首页；root 可进 admin。

**34. XSS 怎么防？**  
答：AI/文章 HTML 经 sanitize 再渲染，避免脚本注入。

**35. CORS 怎么配？**  
答：允许 `*` origins，Bearer 方案不依赖 Cookie credentials。生产可收紧到具体域名。

**36. 知识库文件为何不公开静态挂载？**  
答：仅头像公开；知识文件走鉴权上传/管理接口，防未授权下载。

**37. root 能否删自己？**  
答：业务禁止操作超级管理员账号及当前登录账号，防锁死系统。

**38. 用户名唯一如何保证？**  
答：`username_exists` 跨三表检查，注册/创建前拦截。

**39. 生产安全检查清单？**  
答：改 JWT_SECRET、DB/Neo4j 密码、root 密码策略；公网只暴露前端；HTTPS；备份数据卷。

**40. 若要做刷新令牌怎么扩展？**  
答：短 access + 长 refresh；refresh 存 DB/Redis 可吊销；本项目当前未做，可作改进点。

---

### D. 前端（41–50）

**41. Pinia 存了什么？**  
答：token、userInfo；role 优先从 JWT 解析；登录写入 localStorage；过期初始化时清理。

**42. Axios 拦截器逻辑？**  
答：请求加 Authorization；响应看业务 code；HTTP 401 退出；错误 message 提示。

**43. 为什么 AI 聊天不用 Axios？**  
答：需要可读流逐块消费 SSE；Axios 更适整包 JSON。用 fetch + ReadableStream 解析 `data: {...}\n\n`。

**44. SSE 事件类型有哪些？**  
答：`session`（回传 session_id）、`content`（增量文本）、`done`（引用/图谱/耗时）、`error`。

**45. 管理端与门户布局差异？**  
答：Portal 顶栏导航；Admin/Doctor 侧栏 AdminLayout；菜单随角色切换。

**46. 用户管理序号为何不用数据库 id？**  
答：三表 id 会重复；分页后计算 `seq`；删除后重新加载自然连续。

**47. 分页组件统一做了什么？**  
答：`AppPagination`：总数、翻页、每页条数、跳转；右对齐全站一致。

**48. 主题如何实现？**  
答：`global.css` CSS 变量（品牌色约 `#cc5084`）+ Element Plus 主色覆盖；登录注册侧栏统一变量。

**49. Markdown 渲染注意点？**  
答：markdown-it 渲染后 sanitize，防 XSS；流式追加时注意性能与滚动。

**50. 前端构建产物如何部署？**  
答：多阶段 Dockerfile：Node build → Nginx 托管 dist；反代 `/api` 与 SSE 特殊 location。

---

### E. RAG 与大模型（51–70）

**51. 什么是 RAG？本项目怎么用？**  
答：Retrieval-Augmented Generation：先检索知识片段再生成。本项目 Chroma 检索 Top-K，拼进 Prompt，降低胡编并提供引用。

**52. 知识库入库流水线？**  
答：上传落盘 → `vector_status=1` → loader 解析 → splitter 分块 → 删旧 chunk/向量 → 写 `t_knowledge_chunk` → batch embedding → Chroma → status=2/失败 3。

**53. 分块参数？**  
答：`RecursiveCharacterTextSplitter`，优先中文标点分隔；chunk_size/overlap 来自配置（常见 500/80 量级）。Overlap 保上下文连贯。

**54. 向量 id 如何设计？**  
答：`file_{id}_chunk_{idx}`，便于按文件删除与重建。

**55. 检索用什么相似度？**  
答：Chroma 余弦相关距离；日志打印 distance；前端展示引用来源文件名。

**56. 知识库为空时问诊还能用吗？**  
答：能。上下文变为「暂无相关知识」，仍可走 LLM（质量下降），体现降级。

**57. chat_stream 大致步骤？**  
答：抽症状 → 向量检索 + 图谱推理 → 组装 system/user 消息（含历史截断）→ LLM stream → yield SSE content → done 带 references/graph/cost。

**58. 历史消息如何控制长度？**  
答：取会话历史，发给模型时限制最近若干条（如约 6 条量级），防超上下文窗口。

**59. 为什么对本机 Ollama 关 think？**  
答：Qwen 等模型思考链耗 token；检测到 ollama/localhost 地址且配置关闭 reasoning 时 `extra_body.think=false`。

**60. Ollama 与云端 MaaS 如何切换？**  
答：改 `.env` 的 `OPENAI_BASE_URL`/`API_KEY`/`LLM_MODEL`/`EMBEDDING_*`；Docker 用 `DOCKER_OPENAI_BASE_URL=host.docker.internal:11434/v1`。

**61. 容器访问宿主机 Ollama？**  
答：`extra_hosts: host.docker.internal:host-gateway`；宿主机 `OLLAMA_HOST=0.0.0.0:11434`。

**62. Embedding 与 Chat 能否同一模型？**  
答：不能。对话模型与嵌入模型任务不同；本项目分别配置 `LLM_MODEL` 与 `EMBEDDING_MODEL`。

**63. 异步向量化为何用 BackgroundTasks？**  
答：上传接口快速返回；耗时 embedding 后台跑，避免 HTTP 超时。也可用 Celery/RQ 作进阶。

**64. 向量化失败如何可观测？**  
答：status=3；`[RAG-向量化]` 日志；管理端可重试。

**65. Prompt 如何约束医疗风险？**  
答：系统提示强调参考知识、不确定要说明、严重症状建议就医、不替代诊断。

**66. 引用如何回传前端？**  
答：`done` 事件带 `references`（文件名等）；消息表存 `references_json`。

**67. 流式中途断线怎么办？**  
答：当前以服务端写完再落助手消息为主；断线可能导致前端不完整——可改进为心跳/断点续传（诚实说改进点）。

**68. Nginx 对 SSE 要注意什么？**  
答：`proxy_buffering off`、拉长 `proxy_read_timeout`，否则攒包无流式体验。

**69. 如何评估 RAG 效果？**  
答：人工抽检命中文档是否相关；看 distance；对比有无知识库回答差异；可加离线评测集（改进点）。

**70. 若检索到无关文档怎么办？**  
答：可加相似度阈值过滤、重排序、hybrid 检索；当前实现相对基础，面试可主动提出优化方向。

---

### F. 知识图谱（71–80）

**71. 图谱有哪些节点与关系？**  
答：Disease、Symptom、Department、Drug、Check、Food 等；关系如 HAS_SYMPTOM、BELONGS_TO、RECOMMEND_DRUG、NEED_CHECK、ACCOMPANY_WITH、SHOULD_EAT、AVOID_EAT。

**72. 症状推理算法？**  
答：UNWIND 症状 MATCH HAS_SYMPTOM，按命中症状数 DESC，LIMIT 10；`probability ≈ match_count / 症状总数`；带出科室。

**73. 症状别名如何处理？**  
答：`SYMPTOM_ALIASES` 映射，如发烧→发热、头疼→头痛；归一化后去重保序。

**74. AI 问诊里症状怎么抽？**  
答：内置常见症状词表 + 别名，对用户问题做子串包含匹配，不是 NER。

**75. 为何图谱增强还要 RAG？**  
答：图谱擅长结构化关联与可解释排序；RAG 提供指南类文本细节。二者互补。

**76. Neo4j 不可用时系统？**  
答：共享部署可 `NEO4J_REQUIRED=false`，后端仍可启动；图谱相关功能降级。

**77. 图谱初始化？**  
答：`INIT_GRAPH=true` 时入口脚本跑 `init_graph.py` 导入常见病种。

**78. 管理端图谱可视化？**  
答：ECharts graph force 布局；分类着色；搜索实体看子图。

**79. Cypher 注入风险？**  
答：参数化 `$symptoms` 传入，避免字符串拼接用户输入。

**80. 图谱与业务库科室是否打通？**  
答：图谱 Department 与 MySQL 科室名称层面相关，但是两套数据；深度打通可作演进（统一主数据）。

---

### G. 数据与接口（81–88）

**81. 统一响应格式？**  
答：`{code, message, data}`；分页常含 items/total/page/page_size。

**82. 主要业务表？**  
答：用户三表、科室、知识文件/分块、AI 会话/消息、人工咨询/回复、预约、健康档案、文章、公告。

**83. 为何 ORM 不建 FK？**  
答：灵活迁移；删除靠业务级联。缺点是易漏清——代码审查要盯删除接口。

**84. 用户列表聚合怎么做？**  
答：查三表过滤关键词，按角色优先级与时间排序，内存分页，附加 seq。

**85. 公开接口有哪些？**  
答：登录注册；科室列表；启用医生列表；已发布文章/公告等（以实际路由为准）。

**86. 文件上传限制？**  
答：Nginx `client_max_body_size`（如 50m）；类型限 txt/md/pdf/doc(x)。

**87. 健康档案权限？**  
答：患者只读本人；医生只写/改自己的 `doctor_id` 档案。

**88. 接口文档在哪看？**  
答：FastAPI 自动 `/docs`（Swagger）。

---

### H. 部署运维（89–96）

**89. Compose 服务有哪些？**  
答：frontend、backend、mysql（profile with-mysql）、neo4j（with-graph）。

**90. 三种环境怎么切？**  
答：本地默认 `.env`；生产 `--env-file .env.prod`；共享 MySQL `--env-file .env.shared`。

**91. 后端启动顺序？**  
答：等 MySQL/Neo4j → 建表 → seed → ensure_root → 可选 init_graph/knowledge → uvicorn。

**92. 健康检查？**  
答：backend `/health`；各服务 healthcheck；`depends_on: condition: service_healthy`（共享模式 required:false）。

**93. 数据卷有哪些？**  
答：mysql_data、neo4j_data、chroma_data、uploads_data 等；`down -v` 会清空。

**94. 域名如何挂？**  
答：宿主机 Nginx 反代到容器 WEB_PORT（如 9000）；前端仍相对路径 `/api/v1`，一般不用改业务代码。

**95. 低配 ECS 注意？**  
答：限制 Neo4j/MySQL/backend 内存与 swap；可关图谱或共享 MySQL。

**96. 线上排障三板斧？**  
答：`compose ps` → `logs backend` → 测 Ollama `curl host.docker.internal:11434/api/tags`。

---

### I. 综合与改进（97–100）

**97. 项目最大技术难点？你怎么解的？**  
答：可选其一深讲：① SSE 被 Nginx 缓冲 → 单独 location 关 buffering；② Docker 访 Ollama → host.docker.internal；③ 多表账号列表 → seq+role 路由；④ 向量化失败可观测 → status+日志+重试。

**98. 若重新做，你会改什么？**  
答：引入刷新令牌与登录审计；向量检索加阈值/重排；症状改 NER；消息队列做向量化；补充自动化测试与评测集；图谱与科室主数据统一。

**99. 如何证明你理解业务而不只是调 API？**  
答：能讲清状态机、角色边界、AI 与人工闭环、医疗免责；能区分上传成功 vs 向量化失败；能说明为何 root 不能删自己。

**100. 为什么录用你做类似岗位？**  
答：我完整交付过「业务系统 + RAG/图谱 + 多角色权限 + 容器化部署」闭环，能独立拆需求、控风险、写文档协同；对 LLM 落地的工程坑（流式、嵌入、环境切换）有实战经验，而不仅是调通 Demo。

---

## 四、按岗位的刷题路径

| 岗位 | 优先题号 |
|------|----------|
| Python 后端 | 16–40, 81–96, 97–98 |
| AI / RAG | 4, 8, 51–80, 67–70, 98 |
| 全栈 | 1–50, 88–96 |
| 测试开发 | 5–10, 26–33, 63–64, 89–96 + 状态机表 |
| 运维向 | 18, 21, 61, 68, 89–96 |

---

## 五、面试话术模板（STAR）

**情境 S：** 医疗辅助问诊需要可追溯知识与多角色协作。  
**任务 T：** 独立交付前后端 + RAG + 图谱 + 部署。  
**行动 A：** 分表 RBAC、SSE 问诊、异步向量化、Compose 多环境、文档沉淀。  
**结果 R：** 三角色闭环可演示；知识库状态可运维；本地/云端模型可切换。

---

*文档随代码演进；若实现变更，以仓库与 `/docs` 为准并回改本题库。*

# www.wfrz.fun 域名部署文档

本文说明如何用已备案域名 **www.wfrz.fun** 访问阿里云上的 AI 智能医疗问诊平台（项目目录 `/opt/ai_consulation`）。

当前 ECS 公网 IP：`39.108.180.245`。前端容器对外端口为 **9000**，后端仅绑定本机 `127.0.0.1:8001`，浏览器只应访问前端（Nginx 再反代 `/api`）。

---

## 1. 结论：哪些文件要改、哪些不用改

前端 Axios 使用相对路径 `baseURL: '/api/v1'`，**不写死 IP 或域名**。因此 **不必改 Vue 业务代码**（`request.js`、各页面）。FastAPI CORS 使用 `CORS_ORIGINS` 白名单；同域访问不依赖跨域，若前后端分域部署需显式加入前端域名。

| 路径 | 是否修改 | 作用 |
|------|----------|------|
| `client/nginx.conf` | 建议改 | 容器内 Nginx 识别 `www.wfrz.fun` / `wfrz.fun` |
| `deploy/wfrz.fun.conf` | 新增，拷到服务器 | 宿主机 Nginx 在 **80/443** 反代到容器 `9000` |
| 服务器 `/opt/ai_consulation/.env.shared` | 一般 **不用改 WEB_PORT** | 继续 `WEB_PORT=9000`，由宿主机 80 转到 9000 |
| `docker-compose.yml` | 不用改 | 端口仍由 `.env.shared` 的 `WEB_PORT` 决定 |
| `.env.shared.example` / `.env.prod.example` | 不用为域名改端口 | 文档说明即可 |
| `client/src/utils/request.js` | **不用改** | 已是 `/api/v1` |
| `server/main.py` | **不用改** | CORS 已放行 |

**推荐架构（适配本机 80 已被其它进程占用的现状）：**

```
浏览器
  │  http://www.wfrz.fun        （备案后走 80）
  │  https://www.wfrz.fun       （配证书后走 443）
  ▼
宿主机 Nginx（server_name www.wfrz.fun）
  │  proxy_pass http://127.0.0.1:9000
  ▼
Docker 前端容器 ai-medical-frontend:80
  ├─ /          Vue 静态资源
  ├─ /api/*     → backend:8000
  └─ /uploads33 → backend:8000
```

不要把 Docker 前端直接改绑到宿主机 80：该 ECS 上 80  historically 被其它 Python 服务占用，且 9000 已在 firewalld 放行。域名走 80/443 由**宿主机 Nginx** 承接即可。

---

## 2. 仓库内已做的代码改动

### 2.1 `client/nginx.conf`

将 `server_name` 从通配 `_` 改为包含备案域名（保留 `_` 以免健康检查与 IP 访问失败）：

```nginx
listen 80;
server_name www.wfrz.fun wfrz.fun _;
```

修改后必须 **重建前端镜像** 才会进容器。

### 2.2 `deploy/wfrz.fun.conf`

宿主机 Nginx 站点配置模板，部署时复制到 `/etc/nginx/conf.d/wfrz.fun.conf`。

---

## 3. 阿里云控制台（代码外必须完成）

### 3.1 DNS 解析

登录 [阿里云域名解析](https://dns.console.aliyun.com/)（或域名所在 DNS 服务商），为 `wfrz.fun` 添加：

| 记录类型 | 主机记录 | 记录值 | 说明 |
|----------|----------|--------|------|
| A | `www` | `39.108.180.245` | 访问 www.wfrz.fun |
| A | `@` | `39.108.180.245` | 访问裸域 wfrz.fun（可选） |

TTL 可先用 600 秒。本机验证：

```bash
ping www.wfrz.fun
# 或
nslookup www.wfrz.fun
```

应解析到 `39.108.180.245`。

### 3.2 安全组（必做）

ECS → 安全组 → 入方向放行：

| 端口 | 协议 | 说明 |
|------|------|------|
| 80 | TCP | HTTP（备案站点） |
| 443 | TCP | HTTPS（上证书后） |
| 9000 | TCP | 可选；仅排查时可直连容器 |

**不要**对公网开放 `8001`、`3308`、`7474`、`7687`。

### 3.3 本机 firewalld

```bash
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --reload
firewall-cmd --query-port=80/tcp
firewall-cmd --query-port=443/tcp
```

---

## 4. 服务器上的部署步骤

SSH 登录后执行（项目目录为 `/opt/ai_consulation`）。GitHub 直连不通时继续用代理 remote。

### 4.1 拉取含域名改动的代码

```bash
cd /opt/ai_consulation
chown -R root:root /opt/ai_consulation
git pull --ff-only
```

若 `git pull` 报 `Empty reply from server`：

```bash
git remote set-url origin https://githubproxy.cc/https://github.com/Jadefjg/ai_consulation.git
git pull --ff-only
```

### 4.2 确认应用仍在 9000（不要改成 80）

查看 `/opt/ai_consulation/.env.shared`：

```env
WEB_PORT=9000
BACKEND_PUBLISH=127.0.0.1:8001:8000
```

`MYSQL_PASSWORD` 等必须是 **ASCII 真实密码**，不要用示例里的「与共享库一致」。`COMPOSE_PROFILES` 在 2C2G 上建议留空（不要 `with-graph`）。

### 4.3 重建前端容器（吃进 nginx.conf 的 server_name）

```bash
cd /opt/ai_consulation
docker compose --env-file .env.shared up -d --build --no-deps frontend
docker compose --env-file .env.shared ps
curl -sI http://127.0.0.1:9000/ | head -5
```

### 4.4 安装宿主机反代（让 80 转到 9000）

先确认 80 是否空闲：

```bash
ss -tlnp | grep ':80 '
```

- **若 80 被其它 Python/网站占用：** 停掉或改掉占用 80 的进程，或把该站点改到其它端口，把 **80 留给本域名 Nginx**。备案域名对外标准入口是 80/443。
- **若宿主机已有 Nginx**（本机常见监听 8088、1009）：把配置放进 `/etc/nginx/conf.d/` 即可，不必再装一套。

```bash
cp /opt/ai_consulation/deploy/wfrz.fun.conf /etc/nginx/conf.d/wfrz.fun.conf
nginx -t && systemctl reload nginx
```

本机验证：

```bash
curl -sI -H "Host: www.wfrz.fun" http://127.0.0.1/ | head -8
```

应返回 `200` 且为前端 HTML。

浏览器访问：**http://www.wfrz.fun**

管理员：用户名 `admin`，密码 `feng1010`，身份选「管理员」。

---

## 5. HTTPS（建议备案完成后立即做）

1. 阿里云 SSL 证书 → 免费 DV 证书，绑定 `www.wfrz.fun`（可加 `wfrz.fun`）。
2. 下载 Nginx 版证书，上传到服务器：

```bash
mkdir -p /etc/nginx/ssl
# 将证书放到：
#   /etc/nginx/ssl/wfrz.fun.pem
#   /etc/nginx/ssl/wfrz.fun.key
chmod 600 /etc/nginx/ssl/wfrz.fun.key
```

3. 编辑 `/etc/nginx/conf.d/wfrz.fun.conf`：取消 HTTPS `server { listen 443 ... }` 注释，并在 80 的 `location /` 前增加跳转：

```nginx
return 301 https://$host$request_uri;
```

（保留 `/.well-known/` 若仍用 HTTP 续期。）

4. `nginx -t && systemctl reload nginx`，安全组放行 443。

5. 访问 **https://www.wfrz.fun**。

---

## 6. 日常更新（域名已通之后）

只改前端/后端代码时，**不必改 DNS 和宿主机 Nginx**：

```bash
cd /opt/ai_consulation
git pull --ff-only
docker compose --env-file .env.shared up -d --build --no-deps backend frontend
```

---

## 7. 排障

| 现象 | 处理 |
|------|------|
| 域名能 ping 通但浏览器打不开 | 查安全组 80/443、firewalld、宿主机 Nginx 是否 listen 80 |
| `http://IP:9000` 通、域名不通 | DNS 未生效，或 Host 反代未配、80 被其它进程占用 |
| 页面开了但接口 404 | 确认反代到的是前端容器 9000，而不是直接打后端 8001 |
| 问诊没有流式输出 | 确认宿主机 Nginx 对 `/api/v1/chat/send` 关闭了 `proxy_buffering` |
| 登录后立刻失败 | 仍用 HTTP 与 HTTPS 混用导致 cookie/混合内容；统一用 https |
| `git pull` Empty reply | 使用 `githubproxy.cc` 作为 origin（见 4.1） |

查看占用 80 的进程：

```bash
ss -tlnp | grep ':80 '
```

查看容器：

```bash
docker ps --filter name=ai-medical --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'
```

---

## 8. 检查清单

1. DNS：`www.wfrz.fun` → `39.108.180.245`  
2. 安全组 + firewalld：80（及 443）  
3. `.env.shared` 中 `WEB_PORT=9000`，密码为 ASCII  
4. 已重建 frontend（`server_name` 已进镜像）  
5. `/etc/nginx/conf.d/wfrz.fun.conf` 已启用且 `nginx -t` 通过  
6. http://www.wfrz.fun 可打开登录页  
7. （可选）HTTPS 证书与 301 跳转  

完成后，对外入口即为 **http://www.wfrz.fun**（启用证书后为 **https://www.wfrz.fun**），无需再带 `:9000`。
